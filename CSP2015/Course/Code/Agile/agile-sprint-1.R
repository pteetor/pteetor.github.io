app = function() {
  data = loadData()
  model = fitModel(data)
  summarizeModel(model)
}

loadData = function() {
    NULL
}

fitModel = function(data) {
    NULL
}

summarizeModel = function(model) {
    cat("[TODO - Model summary]\n")
}
